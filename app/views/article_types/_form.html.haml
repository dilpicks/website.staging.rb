= form_for @article_type do |f|
  - if @article_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@article_type.errors.count, "error")} prohibited this article_type from being saved:"
      %ul
        - @article_type.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
